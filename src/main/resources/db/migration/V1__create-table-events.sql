CREATE TABLE events(
    id VARCHAR(255) NOT NULL PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    datails VARCHAR(255) NOT NULL,
    slug VARCHAR(255) NOT NULL,
    maxumum_attendees INTEGER NOT NULL
);

